{
  "name": "dns-recursive-resolver-simulator",
  "version": "1.0.0",
  "description": "DNS recursive resolver simulator with configurable virtual DNS namespace",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "sql.js": "^1.10.3",
    "uuid": "^9.0.1"
  }
}
